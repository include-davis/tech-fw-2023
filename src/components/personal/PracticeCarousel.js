import { useState } from 'react';
/* Cohort TODO: 
   1. import relevant content & styles with a corresponding scss file in styles folder
   2. Look through slides to see how carousel works
   3. Look through example carousel and try to understand each element
   4. When creating your own, provide comments to explain what each element does
   5. Head to personal.jsx, import and render your carousel
*/ 

export default function PracticeCarousel() {

    return (
      <div>

      </div>
    );
}